# AGENTS.md

- Reinstall locally after every change if the change is made in the repo checkout, but not if it is made in a worktree.
- Vocabulary is added with recognition hints only unless explicitly told to also add overrides or replacements.
- All new vocabulary is added to the local config file, not pushed to the public repo.
